{
  "name": "@bunsn/boiler",
  "version": "0.0.1",
  "description": "A tool which scrapes HTML tables on financial institutions' websites.",
  "main": "index.js",
  "scripts": {
    "test": "standard ./*.js lib/*.js lib/**/*.js test/**/*.js index.js && mochify",
    "build": "browserify -s __boiler__ -do dist/boiler.js index.js && uglifyjs -m -c -o dist/boiler.min.js dist/boiler.js"
  },
  "author": "Dom Christie",
  "license": "ISC",
  "devDependencies": {
    "mochify": "^2.10.1",
    "browserify": "^11.0.1",
    "standard": "^5.2.1",
    "uglify-js": "^2.4.24"
  },
  "standard": {
    "global": [
      "Blob",
      "URL",
      "beforeEach",
      "context",
      "describe",
      "it"
    ]
  }
}
